version: "1.0"

defaults:
  base_dir: ../worktress/junior-typing

hooks:
  post_create:
    - type: copy
      from: .claude
      to: .claude
    - type: command
      command: mise trust
    - type: command
      command: mise bootstrap --yes
